spring:
  datasource:
    url: jdbc:h2:mem:user_service;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

eureka:
  client:
    enabled: false

jwt:
  secret: FounderLinkDevSecretKeyMustBeAtLeast32BytesLong!!
