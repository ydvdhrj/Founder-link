eureka:
  client:
    enabled: false

spring:
  cloud:
    config:
      enabled: false
  data:
    mongodb:
      database: founderlink_messages_test
  main:
    allow-bean-definition-overriding: true

